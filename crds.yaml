apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: "2023-04-27T18:11:23Z"
  generation: 1
  name: clusterkopfpeerings.zalando.org
  resourceVersion: "8764"
  uid: fc952366-7185-434d-abaa-d1550ab9c3a3
spec:
  conversion:
    strategy: None
  group: zalando.org
  names:
    kind: ClusterKopfPeering
    listKind: ClusterKopfPeeringList
    plural: clusterkopfpeerings
    singular: clusterkopfpeering
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ClusterKopfPeering
    listKind: ClusterKopfPeeringList
    plural: clusterkopfpeerings
    singular: clusterkopfpeering
  conditions:
  - lastTransitionTime: "2023-04-27T18:11:23Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2023-04-27T18:11:23Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: "2023-04-27T18:11:24Z"
  generation: 1
  name: innodbclusters.mysql.oracle.com
  resourceVersion: "8775"
  uid: ddd2411e-44ed-4a6f-9a06-d2573ae75e5a
spec:
  conversion:
    strategy: None
  group: mysql.oracle.com
  names:
    kind: InnoDBCluster
    listKind: InnoDBClusterList
    plural: innodbclusters
    shortNames:
    - ic
    - ics
    singular: innodbcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Status of the InnoDB Cluster
      jsonPath: .status.cluster.status
      name: Status
      type: string
    - description: Number of ONLINE InnoDB Cluster instances
      jsonPath: .status.cluster.onlineInstances
      name: Online
      type: integer
    - description: Number of InnoDB Cluster instances configured
      jsonPath: .spec.instances
      name: Instances
      type: integer
    - description: Number of Router instances configured for the InnoDB Cluster
      jsonPath: .spec.router.instances
      name: Routers
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        properties:
          metadata:
            properties:
              name:
                maxLength: 40
                type: string
            type: object
          spec:
            properties:
              backupProfiles:
                description: Backup profile specifications for the cluster, which
                  can be referenced from backup schedules and one-off backup jobs
                items:
                  properties:
                    dumpInstance:
                      properties:
                        dumpOptions:
                          description: A dictionary of key-value pairs passed directly
                            to MySQL Shell's DumpInstance()
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        storage:
                          properties:
                            azure:
                              properties:
                                config:
                                  description: Name of a Secret with Azure BLOB Storage
                                    configuration and credentials
                                  type: string
                                containerName:
                                  description: Name of the Azure  BLOB Storage container
                                    where the dump is stored
                                  type: string
                                prefix:
                                  description: Path in the container where the dump
                                    files are stored
                                  type: string
                              required:
                              - containerName
                              - config
                              type: object
                            ociObjectStorage:
                              properties:
                                bucketName:
                                  description: Name of the OCI bucket where backup
                                    is stored
                                  type: string
                                credentials:
                                  description: Name of a Secret with data for accessing
                                    the bucket
                                  type: string
                                prefix:
                                  description: Path in bucket where backup is stored
                                  type: string
                              required:
                              - bucketName
                              - credentials
                              type: object
                            persistentVolumeClaim:
                              description: Specification of the PVC to be used. Used
                                'as is' in pod executing the backup.
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            s3:
                              properties:
                                bucketName:
                                  description: Name of the S3 bucket where the dump
                                    is stored
                                  type: string
                                config:
                                  description: Name of a Secret with S3 configuration
                                    and credentials
                                  type: string
                                endpoint:
                                  description: Override endpoint URL
                                  type: string
                                prefix:
                                  description: Path in the bucket where the dump files
                                    are stored
                                  type: string
                                profile:
                                  default: ""
                                  description: Profile being used in configuration
                                    files
                                  type: string
                              required:
                              - bucketName
                              - config
                              type: object
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    name:
                      description: Embedded backup profile, referenced as backupProfileName
                        elsewhere
                      type: string
                    podAnnotations:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    podLabels:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    snapshot:
                      properties:
                        storage:
                          properties:
                            azure:
                              properties:
                                config:
                                  description: Name of a Secret with Azure BLOB Storage
                                    configuration and credentials
                                  type: string
                                containerName:
                                  description: Name of the Azure  BLOB Storage container
                                    where the dump is stored
                                  type: string
                                prefix:
                                  description: Path in the container where the dump
                                    files are stored
                                  type: string
                              required:
                              - containerName
                              - config
                              type: object
                            ociObjectStorage:
                              properties:
                                bucketName:
                                  description: Bucket name where backup is stored
                                  type: string
                                credentials:
                                  description: Name of a Secret with data for accessing
                                    the bucket
                                  type: string
                                prefix:
                                  description: Path in bucket where backup is stored
                                  type: string
                              required:
                              - bucketName
                              - credentials
                              type: object
                            persistentVolumeClaim:
                              description: Specification of the PVC to be used. Used
                                'as is' in pod executing the backup.
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            s3:
                              properties:
                                bucketName:
                                  description: Name of the S3 bucket where the dump
                                    is stored
                                  type: string
                                config:
                                  description: Name of a Secret with S3 configuration
                                    and credentials
                                  type: string
                                endpoint:
                                  description: Override endpoint URL
                                  type: string
                                prefix:
                                  description: Path in the bucket where the dump files
                                    are stored
                                  type: string
                                profile:
                                  default: ""
                                  description: Profile being used in configuration
                                    files
                                  type: string
                              required:
                              - bucketName
                              - config
                              type: object
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                  required:
                  - name
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
              backupSchedules:
                description: Schedules for periodically executed backups
                items:
                  properties:
                    backupProfile:
                      description: backupProfile specification if backupProfileName
                        is not specified
                      properties:
                        dumpInstance:
                          properties:
                            dumpOptions:
                              description: A dictionary of key-value pairs passed
                                directly to MySQL Shell's DumpInstance()
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            storage:
                              properties:
                                azure:
                                  properties:
                                    config:
                                      description: Name of a Secret with Azure BLOB
                                        Storage configuration and credentials
                                      type: string
                                    containerName:
                                      description: Name of the Azure  BLOB Storage
                                        container where the dump is stored
                                      type: string
                                    prefix:
                                      description: Path in the container where the
                                        dump files are stored
                                      type: string
                                  required:
                                  - containerName
                                  - config
                                  type: object
                                ociObjectStorage:
                                  properties:
                                    bucketName:
                                      description: Name of the OCI Bucket where backup
                                        is stored
                                      type: string
                                    credentials:
                                      description: Name of a Secret with data for
                                        accessing the bucket
                                      type: string
                                    prefix:
                                      description: Path in bucket where backup is
                                        stored
                                      type: string
                                  required:
                                  - bucketName
                                  - credentials
                                  type: object
                                persistentVolumeClaim:
                                  description: Specification of the PVC to be used.
                                    Used 'as is' in pod executing the backup.
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                s3:
                                  properties:
                                    bucketName:
                                      description: Name of the S3 bucket where the
                                        dump is stored
                                      type: string
                                    config:
                                      description: Name of a Secret with S3 configuration
                                        and credentials
                                      type: string
                                    endpoint:
                                      description: Override endpoint URL
                                      type: string
                                    prefix:
                                      description: Path in the bucket where the dump
                                        files are stored
                                      type: string
                                    profile:
                                      default: ""
                                      description: Profile being used in configuration
                                        files
                                      type: string
                                  required:
                                  - bucketName
                                  - config
                                  type: object
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        podAnnotations:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        podLabels:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    backupProfileName:
                      description: Name of the backupProfile to be used
                      type: string
                    deleteBackupData:
                      default: false
                      description: Whether to delete the backup data in case the MySQLBackup
                        object created by the job is deleted
                      type: boolean
                    enabled:
                      default: true
                      description: Whether the schedule is enabled or not
                      type: boolean
                    name:
                      description: Name of the backup schedule
                      type: string
                    schedule:
                      description: The schedule of the job, syntax as a cron expression
                      type: string
                  required:
                  - name
                  - schedule
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
              baseServerId:
                default: 1000
                description: Base value for MySQL server_id for instances in the cluster
                maximum: 4294967195
                minimum: 0
                type: integer
              datadirVolumeClaimTemplate:
                description: Template for a PersistentVolumeClaim, to be used as datadir
                type: object
                x-kubernetes-preserve-unknown-fields: true
              edition:
                description: MySQL Server Edition (community or enterprise)
                pattern: ^(community|enterprise)$
                type: string
              imagePullPolicy:
                description: Defaults to Always, but set to IfNotPresent in deploy-operator.yaml
                  when deploying Operator
                type: string
              imagePullSecrets:
                items:
                  properties:
                    name:
                      type: string
                  type: object
                type: array
              imageRepository:
                description: Repository from where images must be pulled from; defaults
                  to mysql for community and container-registry.oracle.com/mysql for
                  enterprise
                type: string
              initDB:
                properties:
                  clone:
                    properties:
                      donorUrl:
                        description: URL of the cluster to clone from
                        type: string
                      rootUser:
                        default: root
                        description: User name used for cloning
                        type: string
                      secretKeyRef:
                        properties:
                          name:
                            description: Secret name with key 'rootPassword' storing
                              the password for the user specified in rootUser
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - donorUrl
                    - secretKeyRef
                    type: object
                  dump:
                    properties:
                      name:
                        description: Name of the dump. Not used by the operator, but
                          a descriptive hint for the cluster administrator
                        type: string
                      options:
                        description: A dictionary of key-value pairs passed directly
                          to MySQL Shell's loadDump()
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      path:
                        description: Path to the dump in the PVC. Use when specifying
                          persistentVolumeClaim. Omit for ociObjectStorage, S3, or
                          azure.
                        type: string
                      storage:
                        properties:
                          azure:
                            properties:
                              config:
                                description: Name of a Secret with Azure BLOB Storage
                                  configuration and credentials
                                type: string
                              containerName:
                                description: Name of the Azure  BLOB Storage container
                                  where the dump is stored
                                type: string
                              prefix:
                                description: Path in the container where the dump
                                  files are stored
                                type: string
                            required:
                            - containerName
                            - prefix
                            - config
                            type: object
                          ociObjectStorage:
                            properties:
                              bucketName:
                                description: Name of the OCI bucket where the dump
                                  is stored
                                type: string
                              credentials:
                                description: Name of a Secret with data for accessing
                                  the bucket
                                type: string
                              prefix:
                                description: Path in the bucket where the dump files
                                  are stored
                                type: string
                            required:
                            - bucketName
                            - prefix
                            - credentials
                            type: object
                          persistentVolumeClaim:
                            description: Specification of the PVC to be used. Used
                              'as is' in the cloning pod.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          s3:
                            properties:
                              bucketName:
                                description: Name of the S3 bucket where the dump
                                  is stored
                                type: string
                              config:
                                description: Name of a Secret with S3 configuration
                                  and credentials
                                type: string
                              endpoint:
                                description: Override endpoint URL
                                type: string
                              prefix:
                                description: Path in the bucket where the dump files
                                  are stored
                                type: string
                              profile:
                                default: ""
                                description: Profile being used in configuration files
                                type: string
                            required:
                            - bucketName
                            - prefix
                            - config
                            type: object
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - storage
                    type: object
                type: object
                x-kubernetes-preserve-unknown-fields: true
              instances:
                default: 1
                description: Number of MySQL replica instances for the cluster
                maximum: 9
                minimum: 1
                type: integer
              keyring:
                description: Keyring specification
                properties:
                  encryptedFile:
                    description: Keyring 'Encrypted File' specification
                    properties:
                      fileName:
                        description: Full path to the keyring file name inside the
                          storage volume
                        type: string
                      password:
                        description: Name of a secret that contains password for the
                          keyring in the key 'keyring_password'
                        type: string
                      readOnly:
                        default: false
                        description: Whether to open the keyring file in read-only
                          mode
                        type: boolean
                      storage:
                        description: Specification of the volume to be mounted where
                          the keyring file resides
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - fileName
                    - storage
                    - password
                    type: object
                  file:
                    description: Keyring 'File' specification
                    properties:
                      fileName:
                        description: Full path to the keyring file name inside the
                          storage volume
                        type: string
                      readOnly:
                        default: false
                        description: Whether to open the keyring file in read-only
                          mode
                        type: boolean
                      storage:
                        description: Specification of the volume to be mounted where
                          the keyring file resides
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - fileName
                    - storage
                    type: object
                  oci:
                    description: Keyring 'OCI' specification
                    properties:
                      caCertificate:
                        description: Secret that contains ca.crt field with CA certificate
                          bundle file that the keyring_oci plugin uses for Oracle
                          Cloud Infrastructure certificate verification
                        type: string
                      compartment:
                        description: Compartment identifier in the form ocid1.compartment.oc1...
                        pattern: ^ocid1\.compartment\.
                        type: string
                      endpoints:
                        properties:
                          encryption:
                            description: Encryption endpoint URI like {identifier}-crypto.kms.{region}.oraclecloud.com
                            type: string
                          management:
                            description: Management endpoint URI like {identifier}-management.kms.{region}.oraclecloud.com
                            type: string
                          secrets:
                            description: Secrets endpoint URI like secrets.vaults.{region}.oci.oraclecloud.com
                            type: string
                          vaults:
                            description: Vaults endpoint URI like vaults.{region}.oci.oraclecloud.com
                            type: string
                        type: object
                      keyFingerprint:
                        description: Private key fingerprint
                        pattern: ([0-9a-f]{2}:){15}[0-9a-f]{2}$
                        type: string
                      keySecret:
                        description: A secret that contains the private key under
                          the field 'privatekey'
                        type: string
                      masterKey:
                        description: Master key identified in the form ocid1.key.oc1...
                        pattern: ^ocid1\.key\.
                        type: string
                      tenancy:
                        description: Tenancy identifier in the form ocid1.tenancy.oc1...
                        pattern: ^ocid1\.tenancy\.
                        type: string
                      user:
                        description: User identifier in the form of ocid1.user.oc1...
                        pattern: ^ocid1\.user\.
                        type: string
                      virtualVault:
                        description: Vault identifier in the form ocid1.vault.oc1...
                        pattern: ^ocid1\.vault\.
                        type: string
                    required:
                    - user
                    - keySecret
                    - keyFingerprint
                    - tenancy
                    type: object
                type: object
              mycnf:
                description: Custom configuration additions for my.cnf
                type: string
              podAnnotations:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              podLabels:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              podSpec:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              router:
                description: MySQL Router specification
                properties:
                  instances:
                    default: 1
                    description: Number of MySQL Router instances to deploy
                    minimum: 0
                    type: integer
                  podAnnotations:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  podLabels:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  podSpec:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  tlsSecretName:
                    description: Name of a TLS type Secret containing MySQL Router
                      certificate and private key used for SSL
                    type: string
                  version:
                    description: Override MySQL Router version
                    pattern: ^\d+\.\d+\.\d+(-.+)?
                    type: string
                type: object
              secretName:
                description: Name of a generic type Secret containing root/default
                  account password
                type: string
              serviceAccountName:
                type: string
              tlsCASecretName:
                description: Name of a generic type Secret containing CA (ca.pem)
                  and optional CRL (crl.pem) for SSL
                type: string
              tlsSecretName:
                description: Name of a TLS type Secret containing Server certificate
                  and private key for SSL
                type: string
              tlsUseSelfSigned:
                default: false
                description: Enables use of self-signed TLS certificates, reducing
                  or disabling TLS based security verifications
                type: boolean
              version:
                description: MySQL Server version
                pattern: ^\d+\.\d+\.\d+(-.+)?
                type: string
            required:
            - secretName
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: InnoDBCluster
    listKind: InnoDBClusterList
    plural: innodbclusters
    shortNames:
    - ic
    - ics
    singular: innodbcluster
  conditions:
  - lastTransitionTime: "2023-04-27T18:11:24Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2023-04-27T18:11:24Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v2

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: "2023-04-27T18:11:23Z"
  generation: 1
  name: clusterkopfpeerings.zalando.org
  resourceVersion: "8764"
  uid: fc952366-7185-434d-abaa-d1550ab9c3a3
spec:
  conversion:
    strategy: None
  group: zalando.org
  names:
    kind: ClusterKopfPeering
    listKind: ClusterKopfPeeringList
    plural: clusterkopfpeerings
    singular: clusterkopfpeering
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ClusterKopfPeering
    listKind: ClusterKopfPeeringList
    plural: clusterkopfpeerings
    singular: clusterkopfpeering
  conditions:
  - lastTransitionTime: "2023-04-27T18:11:23Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2023-04-27T18:11:23Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1
iaikoroje@cloudshell:~ (me-jeddah-1)$ kubectl get crd kopfpeerings.zalando.org -o yaml
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: "2023-04-27T18:11:23Z"
  generation: 1
  name: kopfpeerings.zalando.org
  resourceVersion: "8768"
  uid: b7b1e3b7-e4fe-4adf-a5a1-ffa9151d92dc
spec:
  conversion:
    strategy: None
  group: zalando.org
  names:
    kind: KopfPeering
    listKind: KopfPeeringList
    plural: kopfpeerings
    singular: kopfpeering
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: KopfPeering
    listKind: KopfPeeringList
    plural: kopfpeerings
    singular: kopfpeering
  conditions:
  - lastTransitionTime: "2023-04-27T18:11:24Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2023-04-27T18:11:23Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: "2023-04-27T18:11:24Z"
  generation: 1
  name: mysqlbackups.mysql.oracle.com
  resourceVersion: "8773"
  uid: a00d4115-5711-4804-a6f3-27012e0f135a
spec:
  conversion:
    strategy: None
  group: mysql.oracle.com
  names:
    kind: MySQLBackup
    listKind: MySQLBackupList
    plural: mysqlbackups
    shortNames:
    - mbk
    singular: mysqlbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Name of the target cluster
      jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - description: Status of the Backup
      jsonPath: .status.status
      name: Status
      type: string
    - description: Name of the produced file/directory
      jsonPath: .status.output
      name: Output
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              addTimestampToBackupDirectory:
                default: true
                type: boolean
              backupProfile:
                description: backupProfile specification if backupProfileName is not
                  specified
                properties:
                  dumpInstance:
                    properties:
                      dumpOptions:
                        description: A dictionary of key-value pairs passed directly
                          to MySQL Shell's DumpInstance()
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      storage:
                        properties:
                          azure:
                            properties:
                              config:
                                description: Name of a Secret with Azure BLOB Storage
                                  configuration and credentials
                                type: string
                              containerName:
                                description: Name of the Azure  BLOB Storage container
                                  where the dump is stored
                                type: string
                              prefix:
                                description: Path in the container where the dump
                                  files are stored
                                type: string
                            required:
                            - containerName
                            - config
                            type: object
                          ociObjectStorage:
                            properties:
                              bucketName:
                                description: Name of the OCI bucket where backup is
                                  stored
                                type: string
                              credentials:
                                description: Name of a Secret with data for accessing
                                  the bucket
                                type: string
                              prefix:
                                description: Path in bucket where backup is stored
                                type: string
                            required:
                            - bucketName
                            - credentials
                            type: object
                          persistentVolumeClaim:
                            description: Specification of the PVC to be used. Used
                              'as is' in pod executing the backup.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          s3:
                            properties:
                              bucketName:
                                description: Name of the S3 bucket where the dump
                                  is stored
                                type: string
                              config:
                                description: Name of a Secret with S3 configuration
                                  and credentials
                                type: string
                              endpoint:
                                description: Override endpoint URL
                                type: string
                              prefix:
                                description: Path in the bucket where the dump files
                                  are stored
                                type: string
                              profile:
                                default: ""
                                description: Profile being used in configuration files
                                type: string
                            required:
                            - bucketName
                            - config
                            type: object
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                  podAnnotations:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  podLabels:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
                x-kubernetes-preserve-unknown-fields: true
              backupProfileName:
                type: string
              clusterName:
                type: string
              deleteBackupData:
                default: false
                type: boolean
            required:
            - clusterName
            type: object
          status:
            properties:
              bucket:
                type: string
              completionTime:
                type: string
              container:
                type: string
              elapsedTime:
                type: string
              message:
                type: string
              method:
                type: string
              ociTenancy:
                type: string
              output:
                type: string
              size:
                type: string
              source:
                type: string
              spaceAvailable:
                type: string
              startTime:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: MySQLBackup
    listKind: MySQLBackupList
    plural: mysqlbackups
    shortNames:
    - mbk
    singular: mysqlbackup
  conditions:
  - lastTransitionTime: "2023-04-27T18:11:24Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2023-04-27T18:11:24Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v2
